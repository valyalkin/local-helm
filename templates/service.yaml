apiVersion: v1
kind: Service
metadata:
  name: {{ include "local-helm.fullname" . }}
  labels:
    {{- include "local-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "local-helm.selectorLabels" . | nindent 4 }}
